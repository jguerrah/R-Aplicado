## -----------------------------------------------------------------------------
objeto.uno <- 1
objeto_uno <- 1
Objeto1 <- 1
Objeto.uno <- 1

## -----------------------------------------------------------------------------
objeto.dos <- 10 # recomendado
objeto_dos = 10
10 -> Objeto2
assign("Objeto.dos", 10)

## -----------------------------------------------------------------------------
objeto_uno
print(objeto_dos)
Objeto1 * 5

## -----------------------------------------------------------------------------
otro_objeto_con_nombre_largo <- 2 * 3
otro_objeto_con_nombre_largo

otro_objeto_con_nombre_largo <- 5 ^ 4
otro_objeto_con_nombre_largo

## -----------------------------------------------------------------------------
seq(1,5)
x <- seq(1,5)
x
#help(seq)

## -----------------------------------------------------------------------------
seq(from = 1, to = 5, by = 2)
seq(from = 1, by = 2, to = 5)
seq(by = 2, to = 5, from = 1)

## -----------------------------------------------------------------------------
y <- rep(0,5)
y
#?rep

## -----------------------------------------------------------------------------
date() # fecha actual

objects() # objetos (variables)
ls() # lista de objetos (variables)

rm(y) # remove object 'y'

## -----------------------------------------------------------------------------
rm(list = ls()) # remove all objects

## -----------------------------------------------------------------------------
getwd() # ver wd
#setwd("C:/Users/Jose/OneDrive/Teaching/UNI") # cambiar wd
#setwd("./Sesion 1") # cambiar a una carpeta del wd
#list.files() # archivos en tu wd

## -----------------------------------------------------------------------------
a <- 5
b <- 6
log <- a < b  # asigna V o F
log
log <- a > b
log
class(log)
log <- FALSE
log

## -----------------------------------------------------------------------------
as.numeric(log)
class(as.numeric(log))
as.logical(0)
class(as.logical(0))

## -----------------------------------------------------------------------------
c(TRUE, FALSE, FALSE, TRUE)
c(1<2, 8>10, 9>23, 100==60+40)

## -----------------------------------------------------------------------------
int <- as.integer(3) # forma correcta
int
class(int)

## -----------------------------------------------------------------------------
int2 <- 5L # forma correcta
int2
class(int2)

## -----------------------------------------------------------------------------
int3 <- as.integer() # forma incorrecta
int3 <- 9
class(int3)

## -----------------------------------------------------------------------------
int4 <- 1:40
int4
class(int4)

## -----------------------------------------------------------------------------
num <- 12.5
num
class(num)

## -----------------------------------------------------------------------------
num2 <- 5 # no necesita decimales para ser numeric
num2
class(num2)

## -----------------------------------------------------------------------------
num3 <- c(0.000001, 3/5, 8.77/4, 1)
num3
class(num3)

## -----------------------------------------------------------------------------
comp <- 12 + 3i # i de imaginario
comp
class(comp)

## -----------------------------------------------------------------------------
char <- as.character("Texto de prueba")
char
class(char)

## -----------------------------------------------------------------------------
char2 <- 'Segundo texto'
char2
class(char2)

## -----------------------------------------------------------------------------
x <- 1.5
y <- -2.7
paste("La coordenada es (",x,",",y,")", sep="")
paste0("La coordenada es (",x,",",y,")") # sep="" por defecto

## -----------------------------------------------------------------------------
char3 <- "3.5"
class(char3)
x <- as.numeric(char3)
x
class(x)

## -----------------------------------------------------------------------------
a <- c(10, 11, 15, 19)
a

## -----------------------------------------------------------------------------
a*a
1/a
b <- a-1
b

## -----------------------------------------------------------------------------
2:10
5:1

## -----------------------------------------------------------------------------
seq(1, 10, 3)
a <- 1:3
b <- rep(a, times=3)
c <- rep(a, each=3)
a; b; c

## -----------------------------------------------------------------------------
b+c
a+c # posible porque 3 es múltiplo de 9
d <- c(10,100)
b + d # warning

## -----------------------------------------------------------------------------
k <- c(1L, 1.999, TRUE, "Hola")
class(k)
k <- c(1L, 1.999, TRUE)
class(k)
k <- c(1L, TRUE)
class(k)
k <- c(TRUE)
class(k)

## -----------------------------------------------------------------------------
a <- matrix(1:12, nrow=3, ncol=4)
a
dim(a) # filas, columnas

## -----------------------------------------------------------------------------
a <- matrix(1:4, nrow=4, ncol=4)
a
dim(a)

## -----------------------------------------------------------------------------
a <- matrix(1:9, nrow=4, ncol=4) # warning
a
dim(a)

## -----------------------------------------------------------------------------
z <- array(1:24, dim=c(2,3,4)) # 3 dimensiones
z
dim(z)

## -----------------------------------------------------------------------------
z <- array(1:16, dim=c(2,2,2,2)) # 4 dimensiones
z
dim(z)

## -----------------------------------------------------------------------------
gal <- list(name="NGC3379", morf="E", T.RC3=-5, colours=c(0.53,0.96))
gal
length(gal)
names(gal)

## -----------------------------------------------------------------------------
gal$radio <- TRUE
gal$redshift <- 0.002921
names(gal)
gal

## -----------------------------------------------------------------------------
gal2 <- list(matrix(1:4, nrow=2, ncol=2), c(TRUE, FALSE))
gal2

## -----------------------------------------------------------------------------
gal3 <- c(gal, gal2)
gal3
class(gal3)
names(gal3)

## -----------------------------------------------------------------------------
x <- list(list(list(list())))
x

## -----------------------------------------------------------------------------
x <- 1:20
w <- 1 + sqrt(x)/2
dummy <- data.frame(x=x, y=x + rnorm(x)*w)
dummy
class(dummy)
dim(dummy)
length(dummy)

## -----------------------------------------------------------------------------
df <- data.frame(numeros=c(10,20,30,40),
                 texto=c("a","b","a","c"))
df
df$texto

## -----------------------------------------------------------------------------
df <- data.frame(numeros=c(10,20,30,40),
                 texto=c("a","b","a","c"))
df
df$texto

## -----------------------------------------------------------------------------
rm(list = ls()) # limpiando el global enviroment

## -----------------------------------------------------------------------------
my_function <- function() {
  print("Hola!")
}
my_function()

## -----------------------------------------------------------------------------
f1 <- function(w, z) {
  # Comentarios de la funci?n
  w + z + 10
}

## -----------------------------------------------------------------------------
formals(f1) # inputs
body(f1)
environment(f1)

## -----------------------------------------------------------------------------
x <- 10
y <- 20
f1(x,y)

## -----------------------------------------------------------------------------
rating <- 29
clasificacion <- if (rating > 70) {
  "A"
  } else if (rating > 50) {
  "B"
  } else {
  "c"
}
clasificacion

